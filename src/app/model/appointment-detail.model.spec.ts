import { AppointmentDetail } from './appointment-detail.model';

describe('AppointmentDetail', () => {
  it('should create an instance', () => {
    expect(new AppointmentDetail()).toBeTruthy();
  });
});
